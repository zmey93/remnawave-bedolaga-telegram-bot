# ФАЙЛ ОТКЛЮЧЕН — заменён на zmey-release.yml
# Release Please не поддерживает формат vX.Y.ZzN
name: Release Please (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use zmey-release.yml instead.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Release Please disabled. Use zmey-release.yml instead.'
