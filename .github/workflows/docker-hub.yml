# ФАЙЛ ОТКЛЮЧЁН — форк zmey93 не использует Docker Hub
# Оригинал пушил в fr1ngg/ на Docker Hub, у нас нет секретов DOCKER_USERNAME/DOCKER_PASSWORD
# Сборка идёт через docker-registry.yml в GHCR (ghcr.io/zmey93/)
name: BedolagaBot Docker Hub (disabled)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Disabled — no Docker Hub secrets in fork zmey93'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Docker Hub workflow disabled. Images are pushed to GHCR via docker-registry.yml'
